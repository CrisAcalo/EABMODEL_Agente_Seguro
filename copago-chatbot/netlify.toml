[build]
  command   = "npm run build"
  publish   = "dist"

# SPA redirect: all routes → index.html (needed for React Router and direct URL access)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
